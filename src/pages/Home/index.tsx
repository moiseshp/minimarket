import React from 'react'

const Home: React.FC = () => {
  // React.useEffect(() => {
  //   dispatch(shopActions.getShop())
  // }, [dispatch])

  // if (!shop.uuid) {
  //   return <Spinner p={2} />
  // }

  return (
    <React.Fragment>
      Landing Page
    </React.Fragment>
  )
}

export default Home
